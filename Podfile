# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'DeenBytes' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for DeenBytes
  pod 'SwiftGif', '~> 1.2.0'

  target 'DeenBytesTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'DeenBytesUITests' do
    # Pods for testing
  end
end
